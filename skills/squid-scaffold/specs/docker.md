---
name: docker
description: Docker opinions — slim Dockerfiles (multi-stage builds, non-root user, minimal base images, labels, healthcheck) and docker-compose (one service per component, healthchecks, shared network, .env layering, profiles for optional services). TRIGGER when writing or reviewing a Dockerfile or docker-compose.yml. SKIP for Kubernetes / ECS / Nomad manifests.
---

# Docker

> **Stub — not yet written.**
